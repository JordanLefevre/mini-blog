{% extends 'base.html.twig' %}

{% block body %}
    <div class="row form-title"> 
        <div class="col s12">
            <h1>Catégorie</h1>
        </div>
    </div>
    <div class="row form-content">
        <div class="col s12">
            <table class="table-show">
                <tbody>
                    <tr>
                        <th class="th-show">Id</th>
                        <td class="td-show">{{ category.id }}</td>
                    </tr>
                    <tr>
                        <th class="th-show">Nom</th>
                        <td class="td-show">{{ category.name }}</td>
                    </tr>
                </tbody>
            </table>
            <div class="row" style="margin-bottom:0">
                <div class="col s12 m4 l2">
                    <a href="{{ path('administration_category_index') }}" class="waves-effect waves-light btn teal darken-3 col s12">Retour</a>
                </div>
                <div class="col s12 m8 l4 right" id="submit">   
                    <a href="{{ path('administration_category_edit', { 'id': category.id }) }}" class="waves-effect waves-light btn col s12 m6 l5 right teal darken-3">Modifier</a>            
                </div>
            </div>
            <div class="row">
                <div class="col s12 m4 l2">
                    {{ form_start(delete_form) }}
                        <input type="submit" class="waves-effect waves-light btn col s12 red" value="Supprimer">
                    {{ form_end(delete_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
